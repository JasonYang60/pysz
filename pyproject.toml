[build-system]
requires = ["setuptools", "wheel",
"setuptools_scm", "cython>=3.0.10",
"requests"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_scheme = "guess_next_dev"
local_scheme = "node_and_date"